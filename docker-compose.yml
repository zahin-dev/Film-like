services:
  db:
    image: postgres:16
    container_name: cinemood_db
    environment:
      POSTGRES_USER: cinemood
      POSTGRES_PASSWORD: cinemood
      POSTGRES_DB: cinemood
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
