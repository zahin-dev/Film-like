// Entry point for the AuthPage module.
// App.jsx imports from ./pages/AuthPage (this file).
// We re-export the actual component so the folder structure stays clean.
export { default } from './AuthPage'
